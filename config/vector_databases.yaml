vector_databases:
  main:
    chroma_path: data/chroma/main
    bm25_path: data/bm25
    collection_name: main
    embedding_model: all-MiniLM-L6-v2
    # Weights tune the RRF fusion bias between sparse/dense.
    # Defaults are un-tuned; run eval across a grid once available.
    sparse_weight: 0.4
    dense_weight: 0.6
